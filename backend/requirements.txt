# Core Application Dependencies
# Only core packages are constrained. Transitive dependencies are resolved by uv.

# Web Framework
fastapi>=0.121.0
uvicorn[standard]>=0.32.0

# Database Drivers
neo4j>=5.26.0
redis>=5.0.0

# Configuration
python-dotenv>=1.0.0
pydantic-settings>=2.6.0

# LangChain AI Integration (Latest stable versions)
langchain>=1.2.0
langchain-openai>=1.1.0
langchain-neo4j>=0.6.0
langgraph>=1.0.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# Note: Transitive dependencies like numpy, python-multipart, etc.
# are automatically resolved by uv based on core package requirements.
