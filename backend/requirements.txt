# Core Application Dependencies
# Only core packages are constrained. Transitive dependencies are resolved by uv.

# Web Framework
fastapi>=0.121.0
uvicorn[standard]>=0.32.0

# Database Drivers
neo4j>=5.26.0
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.30
redis>=5.0.0

# Configuration
python-dotenv>=1.0.0
pydantic-settings>=2.6.0

# AI/ML
sentence-transformers>=2.3.1,<6.0.0
litellm>=1.60.0

# Background Tasks
arq>=0.26.3

# Utilities
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# Note: Transitive dependencies like numpy, python-multipart, etc.
# are automatically resolved by uv based on core package requirements.
